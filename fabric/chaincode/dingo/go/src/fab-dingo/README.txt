GoDocs were actually created for the fab_dingo package, because the main function needs to work by calling the package main. However the functionality is exactly the same

To view Code Documentation with fully functioning links is:

http://localhost:6060/pkg/fab_dingo/?m=all
